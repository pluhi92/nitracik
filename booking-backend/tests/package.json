{
  "name": "nitracik-tests",
  "version": "1.0.0",
  "description": "Integračné testy pre Nitráčik backend",
  "scripts": {
    "test": "cross-env TEST_DATABASE_URL=postgresql://postgres:polipok@localhost:5432/nitracik_test DB_NAME=nitracik_test jest --runInBand --detectOpenHandles --forceExit",
    "test:watch": "cross-env TEST_DATABASE_URL=postgresql://postgres:polipok@localhost:5432/nitracik_test DB_NAME=nitracik_test jest --watch --runInBand",
    "test:coverage": "cross-env TEST_DATABASE_URL=postgresql://postgres:polipok@localhost:5432/nitracik_test DB_NAME=nitracik_test jest --coverage --runInBand --forceExit"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/*.test.js"
    ],
    "setupFilesAfterEnv": [
      "./setup.js"
    ],
    "testTimeout": 30000
  }
}
